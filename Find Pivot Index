class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        tot=sum(nums)
        left=0
        for i in range(len(nums)):
            if left == tot-left-nums[i]:
                return i
            left+=nums[i]    
        return -1    
