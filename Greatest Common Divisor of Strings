class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        if str1 + str2  != str2 + str1:
            return ""
        s1=len(str1) 
        s2=len(str2)
        def gcd(s1,s2) :
            return s1 if s2 == 0 else gcd(s2,s1%s2)
        return str1[:gcd(s1,s2)]    
