class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        pre=[0]*(len(gain)+1)
        pre[0] = 0
        for i in range(1,len(gain)+1 ):
            pre[i] = pre[i-1] + gain[i-1]
        return max(pre)  
